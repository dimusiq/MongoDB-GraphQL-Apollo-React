declare type PanelPayload = {
    calendar?: boolean;
    month?: boolean;
    year?: boolean;
};
declare type PanelAction = {
    type: 'change';
    payload: {
        key: 'previous' | 'next';
        value: PanelPayload;
    };
} | {
    type: 'multiple';
    payload: any;
};
declare type PanelState = {
    previous: {
        calendar: boolean;
        month: boolean;
        year: boolean;
    };
    next: {
        calendar: boolean;
        month: boolean;
        year: boolean;
    };
};
declare const panelReducer: (state: PanelState, action: PanelAction) => any;
export default panelReducer;
